{% extends 'UserSite/layout.html.twig' %}
{% block body %}
    <h1>{% trans %}user_site_blog_header{% endtrans %}</h1>
    {% for post in posts %}
        <h2>{% if post.translatedTitle[app.request.locale] is defined %}{{ post.translatedTitle[app.request.locale] }}{% endif %}</h2>
        <a href="{{ path('user_admin_post_edit', {site:post.site.id, id: post.id}) }}">edit</a>
    {% endfor %}
    </p>
{% endblock %}
